// Ternary operator
// condition ? true statement : false statement

const a = 12
// console.log('' ? 'true statement' : 'false statement');


// And operator &&
let color = 'blue'
let size = 24

console.log(color !== 'red' && size > 30 ? 'true statement' : 'false statement');
console.log(color !== 'red' || (size > 30 && size < 50) ? 'true statement' : 'false statement');

